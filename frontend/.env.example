# Backend API base URL (no trailing slash). Leave empty to use http://localhost:5000 in dev.
VITE_API_URL=http://localhost:5000
# Optional: debug | info | warn | error (default: info in dev, warn in prod)
# VITE_LOG_LEVEL=debug
